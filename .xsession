#!/bin/bash

set -euo pipefail
IFS=$'\n\t'

# readonly HOSTNAME="$(hostname)"
# readonly SCREEN_SETUP_SCRIPT="${HOME}/Scripts/screen-setup/profile-${HOSTNAME}-default"


# if [ -x "${SCREEN_SETUP_SCRIPT}" ]; then
#   ${SCREEN_SETUP_SCRIPT}
# fi
